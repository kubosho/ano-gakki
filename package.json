{
  "directories": {
    "test": "./test/"
  },
  "devDependencies": {
    "bower": "^1.3.12",
    "browser-sync": "^2.1.1",
    "browserify": "^10.1.3",
    "coveralls": "^2.11.2",
    "del": "^1.1.0",
    "dtsm": "^0.9.2",
    "espower-loader": "^0.11.0",
    "gulp": "^3.8.10",
    "gulp-eslint": "^0.11.1",
    "gulp-espower": "^0.10.0",
    "gulp-istanbul": "^0.9.0",
    "gulp-load-plugins": "^0.10.0",
    "gulp-mocha": "^2.0.0",
    "gulp-typescript": "^2.3.0",
    "intelli-espower-loader": "^0.7.0",
    "power-assert": "^0.11.0",
    "run-sequence": "^1.0.2",
    "typescript": "^1.4.1",
    "vinyl-source-stream": "^1.0.0",
    "web-audio-test-api": "^0.3.0"
  },
  "scripts": {
    "setup": "npm install && bower install && dtsm install",
    "setup-ci": "npm install && bower install",
    "compile": "gulp compile",
    "clean": "gulp clean",
    "test": "gulp espower && gulp test",
    "browserify": "gulp browserify",
    "build": "gulp build",
    "start": "gulp serve"
  },
  "private": true
}
