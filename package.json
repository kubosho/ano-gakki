{
  "directories": {
    "test": "./test/"
  },
  "devDependencies": {
    "browser-sync": "^2.1.1",
    "browserify": "^8.1.3",
    "coveralls": "^2.11.2",
    "del": "^1.1.0",
    "dtsm": "^0.6.2",
    "espower-loader": "^0.10.0",
    "glob": "^4.3.2",
    "gulp": "^3.8.10",
    "gulp-concat": "^2.4.2",
    "gulp-espower": "^0.10.0",
    "gulp-istanbul": "^0.6.0",
    "gulp-jshint": "^1.9.0",
    "gulp-load-plugins": "^0.8.0",
    "gulp-mocha": "^2.0.0",
    "gulp-typescript": "^2.3.0",
    "intelli-espower-loader": "^0.6.0",
    "jshint-stylish": "^1.0.0",
    "power-assert": "^0.10.0",
    "run-sequence": "^1.0.2",
    "vinyl-source-stream": "^1.0.0",
    "web-audio-test-api": "^0.2.1"
  },
  "scripts": {
    "global-setup": "npm install -g bower typescript",
    "setup": "npm install && bower install && dtsm install",
    "compile": "gulp compile",
    "clean": "gulp clean",
    "copy": "cp -f dist/**/*.js dist",
    "test": "gulp lint && gulp espower && gulp test",
    "build": "gulp browserify",
    "serve": "gulp serve"
  },
  "private": true
}
